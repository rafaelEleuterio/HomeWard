services:

  postgres:
    image: postgres:17
    container_name: homeward-postgres

    environment:
      POSTGRES_DB: homeward
      POSTGRES_USER: homeward
      POSTGRES_PASSWORD: homeward

    ports:
      - "15432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: