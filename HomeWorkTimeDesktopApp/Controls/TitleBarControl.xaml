<UserControl
    x:Class="HomeWorkTimeDesktopApp.Controls.TitleBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:HomeWorkTimeDesktopApp.Infrastructure.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HomeWorkTimeDesktopApp.Controls"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Border
        Height="40"
        Background="{DynamicResource MaterialDesign.Brush.Primary.Dark}"
        BorderBrush="{DynamicResource MaterialDesign.Brush.Primary.Dark}"
        BorderThickness="0"
        MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <DockPanel LastChildFill="False" VerticalAlignment="Center">
            <TextBlock
                Margin="10,0,0,0"
                DockPanel.Dock="Left"
                VerticalAlignment="Center"
                FontSize="16"
                FontWeight="Bold"
                Foreground="{DynamicResource MaterialDesign.Brush.Primary.Dark.Foreground}"
                Text="{Binding MainTitle}" />

            <Button
                Command="{Binding CloseCommand}"
                DockPanel.Dock="Right"
                Height="40"
                Style="{StaticResource CloseButton}"
                Visibility="{Binding CloseVisible, Converter={StaticResource BoolToVisibility}}">
                <materialDesign:PackIcon Kind="Close" />
            </Button>
            <Button
                Command="{Binding MaximizeCommand}"
                DockPanel.Dock="Right"
                Height="40"
                Style="{StaticResource TitleBarButton}"
                Visibility="{Binding MaximizeVisible, Converter={StaticResource BoolToVisibility}}">
                <materialDesign:PackIcon Kind="WindowMaximize" />
            </Button>
            <Button
                Command="{Binding MinimizeCommand}"
                Height="40"
                DockPanel.Dock="Right"
                Style="{StaticResource TitleBarButton}"
                Visibility="{Binding MinimizeVisible, Converter={StaticResource BoolToVisibility}}">
                <materialDesign:PackIcon Kind="Minus" />
            </Button>
        </DockPanel>
    </Border>
</UserControl>
